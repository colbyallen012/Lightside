import React, { Component } from 'react';
import Button from './Button';
import './NavBar.css';

const NavBar = props => {
  return (
    <section>
      <Button />
      <Button />
      <Button />
    </section>
  )
}

export default NavBar;